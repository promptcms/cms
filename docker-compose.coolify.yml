services:
  app:
    image: ghcr.io/promptcms/cms:latest
    container_name: promptcms
    restart: unless-stopped
    ports:
      - "${APP_PORT:-80}:80"
    environment:
      # ":80" disables FrankenPHP HTTPS — Coolify handles SSL termination
      SERVER_NAME: ":80"
      APP_NAME: "${APP_NAME:-PromptCMS}"
      APP_ENV: "${APP_ENV:-production}"
      APP_KEY: "${APP_KEY}"
      APP_DEBUG: "${APP_DEBUG:-false}"
      APP_URL: "${APP_URL}"
      DB_CONNECTION: "${DB_CONNECTION:-sqlite}"
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
      QUEUE_CONNECTION: "${QUEUE_CONNECTION:-database}"
      CACHE_STORE: "${CACHE_STORE:-database}"
      SESSION_DRIVER: "${SESSION_DRIVER:-database}"
      CMS_DEFAULT_LOCALE: "${CMS_DEFAULT_LOCALE:-de}"
      CMS_ENABLE_MCP: "${CMS_ENABLE_MCP:-false}"
    volumes:
      - db-data:/app/database
      - storage-data:/app/storage/app
      - plugins-data:/app/plugins
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/up"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s


volumes:
  db-data:
  storage-data:
  plugins-data:
